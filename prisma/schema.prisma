// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // or "mysql" / "sqlite"
  url      = env("DATABASE_URL")
}

model GlobalSettings {
  id           String  @id @default(cuid())
  isDropActive Boolean @default(false)
}